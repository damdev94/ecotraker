<%= simple_form_for(vehicle, html: { id: 'vehicleForm' }) do |f| %>

  <%= f.input :brand %>
  <%= f.input :model %>
  <%= f.input :year %>
  <%= f.submit class: "button-green button-container secondary-title" %>

<% end %>
